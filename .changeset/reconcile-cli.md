---
'tollstile': minor
---

`npx tollstile reconcile`: run reconciliation from cron, CI, or a terminal, with `--older-than`, `--json`, and `--fail-on-pending`. `reconcile()` reports now list each examined charge with its state before and after, and the errors reported during the run.
