---
'@tollstile/proxy': minor
---

New package: a paid gateway in front of any HTTP API or MCP server. Charge per route or per MCP tool without code changes in the upstream service (FastAPI, the MCP Python SDK, Go, Rails), with canonical path matching, payment headers for the upstream, variable amounts reported by header, and receipts added to tool results.
