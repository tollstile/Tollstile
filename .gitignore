# AI agent instructions (local only)
AGENTS.md
CLAUDE.md
CLAUDE.local.md
.claude/

# Dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# Environment
.env
.env.*
!.env.example

# Local databases (e.g. examples/hono's SQLite ledger)
*.db
*.db-shm
*.db-wal

# Local worker state (miniflare's D1, Durable Objects, and build output)
.wrangler/

# OS
.DS_Store

# Website (deployed to Cloudflare Pages directly)
web/

# Research snapshots (large, regenerated by research/sparse-settlement/fetch-bazaar.mjs)
research/*/snapshots/
